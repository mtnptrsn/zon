{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/socket-server",
      "packages/shared"
    ]
  },
  "scripts": {
    "build": "yarn workspace socket-server build",
    "start": "yarn workspace socket-server start"
  }
}
